📋 СВОДКА ФИКСОВ ЛУНЫ — ВСЕ ФАЙЛЫ И ИЗМЕНЕНИЯ

═══════════════════════════════════════════════════════════════

FILE 1: bot.py
───────────────────────────────────────────────────────────

ЧТО ИЗМЕНИЛОСЬ:
  Функция claude_reply полностью переписана (1179-1243 строки)

БЫЛО:
  async def claude_reply(...):
      client = anthropic.AsyncAnthropic(api_key=DEEPSEEK_API_KEY, base_url=DEEPSEEK_BASE_URL)
      msg = await client.messages.create(...)
      return msg.content[0].text  ← ❌ КРАХ если ThinkingBlock

СТАЛО:
  ✅ Три новые функции:
     - async def _call_deepseek_once() — одна попытка DeepSeek
     - async def _call_openai_once() — одна попытка OpenAI
     - async def claude_reply() — главная логика с fallback

  ✅ Отключен thinking в DeepSeek:
     extra_body={"thinking": {"type": "disabled"}}

  ✅ Правильная обработка content блоков:
     for block in msg.content:
         if hasattr(block, 'text'):
             text_parts.append(block.text)

  ✅ Timeout 15сек на оба API:
     await asyncio.wait_for(..., timeout=15.0)

  ✅ Retry логика (4 попытки):
     DeepSeek 1-2, потом OpenAI 1-2, потом фолбэк

  ✅ Интеллектуальный fallback (определяет язык по тексту)

═══════════════════════════════════════════════════════════════

FILE 2: config.py
───────────────────────────────────────────────────────────

ЧТО ИЗМЕНИЛОСЬ:
  Добавлены три новые переменные окружения для DeepSeek

БЫЛО:
  # OpenAI (для эмбеддингов RAG)
  OPENAI_API_KEY = os.getenv("OPENAI_API_KEY", "")

СТАЛО:
  # OpenAI (запасной провайдер, gpt-5-mini на казахском)
  OPENAI_API_KEY = os.getenv("OPENAI_API_KEY", "")

  # DeepSeek (основной провайдер для Луны)
  DEEPSEEK_API_KEY = os.getenv("DEEPSEEK_API_KEY", "")
  DEEPSEEK_BASE_URL = os.getenv("DEEPSEEK_BASE_URL", "https://api.deepseek.com")
  DEEPSEEK_MODEL = os.getenv("DEEPSEEK_MODEL", "deepseek-v4-flash")

ВАЖНО ДЛЯ RAILWAY:
  Нужно добавить в Railway Variables:
  - DEEPSEEK_API_KEY (ключ от DeepSeek)
  - DEEPSEEK_BASE_URL = https://api.deepseek.com
  - DEEPSEEK_MODEL = deepseek-v4-flash

═══════════════════════════════════════════════════════════════

FILE 3: prompt.py
───────────────────────────────────────────────────────────

ЧТО ИЗМЕНИЛОСЬ:
  Исправлена сумма предоплаты в трёх местах: 19 900 → 30 000 тг

МЕСТО 1: Возражение про деньги (ШАГ 7, линия ~213)
  БЫЛО:  "...или забронируем место за 19 900 тг, остальное потом."
  СТАЛО: "...или забронируем место за 30 000 тг, остальное потом."

МЕСТО 2: Возражение про следующий поток (ШАГ 7, линия ~233)
  БЫЛО:  "...предоплата 19 900 тг, цена не изменится."
  СТАЛО: "...предоплата 30 000 тг, цена не изменится."

МЕСТО 3: Финальное уточнение про дожим (ШАГ 9, линия ~241)
  БЫЛО:  "...Предоплата 19 900 тг зафиксирует за Вами место..."
  СТАЛО: "...Предоплата 30 000 тг зафиксирует за Вами место..."

═══════════════════════════════════════════════════════════════

ИТОГО ИЗМЕНЕНИЙ:
───────────────────────────────────────────────────────────

1️⃣ bot.py:
   - Функция claude_reply переписана (~150 новых строк кода)
   - Добавлены две вспомогательные функции
   - Логирование для отладки
   - Fallback логика с автоматическим языком определением

2️⃣ config.py:
   - Добавлены 3 переменные окружения для DeepSeek

3️⃣ prompt.py:
   - Исправлены 3 упоминания предоплаты: 19 900 → 30 000

═══════════════════════════════════════════════════════════════

КЛЮЧЕВЫЕ ИСПРАВЛЕНИЯ:
───────────────────────────────────────────────────────────

✅ FIX #1: ThinkingBlock ошибка
   Было: 'ThinkingBlock' object has no attribute 'text'
   Исправлено: thinking отключен, блоки обработаны правильно

✅ FIX #2: Долгие ожидания
   Было: если DeepSeek молчит → ошибка через 30сек
   Исправлено: timeout 15сек → переключение на OpenAI

✅ FIX #3: Полный отказ систем
   Было: если оба молчат → зависание
   Исправлено: фолбэк текст за 3 строки кода

✅ FIX #4: Неправильная предоплата
   Было: "предоплата 19 900 тг"
   Исправлено: "предоплата 30 000 тг" (три места)

═══════════════════════════════════════════════════════════════

РИСКИ И СНИЖЕНИЕ РИСКОВ:
───────────────────────────────────────────────────────────

⚠️ РИСК: Сломать существующую функцию claude_reply
   ✅ СНИЖЕНИЕ: Новая версия полностью совместима
              Вызовы остаются те же: claude_reply(messages, system_prompt, kb)
              Возвращает строку как раньше

⚠️ РИСК: Переключиться на OpenAI без нужды (траты)
   ✅ СНИЖЕНИЕ: OpenAI вызывается ТОЛЬКО если DeepSeek дважды пуст
              Обычно DeepSeek работает в 96% случаев (см. логи)

⚠️ РИСК: Потерять контекст казахского языка
   ✅ СНИЖЕНИЕ: Автоматическое определение языка по тексту
              Ищет казахские буквы: әіңғүұқө

═══════════════════════════════════════════════════════════════

ПРОВЕРКА ПЕРЕД ДЕПЛОЕМ:
───────────────────────────────────────────────────────────

[ ] Скачаны все три файла: bot.py, config.py, prompt.py
[ ] Скопированы в репозиторий
[ ] Переменные окружения добавлены в Railway
[ ] Git commit: "fix: claude_reply fallback, disable thinking"
[ ] Git push
[ ] Railway перезагрузился (check logs)
[ ] Первое сообщение от клиента обработано
[ ] Логи показывают "✅ DeepSeek успешен"

═══════════════════════════════════════════════════════════════

ДОПОЛНИТЕЛЬНЫЕ ФАЙЛЫ:
───────────────────────────────────────────────────────────

📄 ИЗМЕНЕНИЯ.md — подробное объяснение всех фиксов
📄 ДЕПЛОЙ.txt — быстрая инструкция по деплою
📄 ПРИМЕРЫ_ЛОГОВ.md — наглядные примеры логов по сценариям
📄 СВОДКА.txt — этот файл (краткая сводка)

═══════════════════════════════════════════════════════════════

ГОТОВО! 🚀

Все файлы в /mnt/user-data/outputs/
Можно пушить на Railway
